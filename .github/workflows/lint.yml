on:
  pull_request:
    branches:
    - main
jobs:
  lint:
    runs-on: ubuntu-18.04
    steps:
      - name: Setup Node
        uses: actions/setup-node@v2
        with:
          node-version: '14.x'
      - run: ls -la
      - run: node -v
      - run: npm -v
